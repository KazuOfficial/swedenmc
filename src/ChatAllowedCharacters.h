#ifndef CHAT_ALLOWED_CHARACTERS_H
#define CHAT_ALLOWED_CHARACTERS_H

extern char *ChatAllowedCharacters_allowedCharacters;

extern const char ChatAllowedCharacters_allowedCharactersArray[];
extern const int ChatAllowedCharacters_allowedCharactersArray_length;

void ChatAllowedCharacters_init(void);

#endif
